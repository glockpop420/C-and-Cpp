#include<stdio.h> 
int main()
{
	char ch;  //1000 (memory location) = ch
	scanf("%c",&ch); // format specifier -> in which format the ch have to take while compiling
	printf("%c\n",ch); //&ch - address ch have to store
}

